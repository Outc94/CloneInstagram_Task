<div id="main2">
<div id="left">
&nbsp
</div>
<div id="content">
<div class="content-content">
<% if logged_in? %>
<h1>Welcome <%= @user.full_name %> !</h1>
<p>Your email Address is: <%= @user.email %></p>
</div>
<div class="content-content">
<% else %>
<h1>Welcome <%= @user.full_name %> ! </h1>
<p>Your email Address is: <%= @user.email %></p>
<p>Now you can LOGIN below !</p>
<button class="btn btn-success"><%= link_to "Log in", new_session_path %></button>
<% end %>
<% flash.each do |key, value| %>
    <%= content_tag(:div, value, class: "#{key}") %>
  <% end %>
</div>
<div id="right"></div>
</div>
</div>
